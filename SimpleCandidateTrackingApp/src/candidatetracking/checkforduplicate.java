package candidatetracking;
/*import java.sql.*;
import java.util.List;*/

public class checkforduplicate {
	/*final String JDBC_DRIVER="com.mysql.jdbc.Driver";  
    final String DB_URL="jdbc:mysql://localhost:3306/insight4excel";

    //  Database credentials
    final String USER = "root";
    final String PASS = "2929";
    Connection conn = null;
    Statement stmt = null;
    //String stremail = request.get
    String sqlstr = "select count(*) from candidatedetails where email_id =? "
    	      
    try{
        // Register JDBC driver
        Class.forName(JDBC_DRIVER);
       
         // Open a connection
        conn = DriverManager.getConnection(DB_URL,USER,PASS);
        stmt= conn.createStatement();
       
        ResultSet rs= stmt.executeQuery(sqlstr);
       // List<String> colnames = getColumnNames(rs);
        
        
    }catch(SQLException se){
        //Handle errors for JDBC
        se.printStackTrace();
     }catch(Exception e){
        //Handle errors for Class.forName
        e.printStackTrace();
     }finally{
        //finally block used to close resources
        
      	  try {
      		  if (conn!= null)
  	          {
				conn.close();
  	          }
			} catch (SQLException e) {
				// TODO Auto-generated catch block
				e.printStackTrace();
			}
     } //end try	      

	}*/
}
